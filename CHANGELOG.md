# Changelog

本项目遵循语义化版本号。

## [0.4.5] - 2026-09-09

- 教师节已识别题面允许一个选项内的小范围彩色鼠标遮挡，保持当前框选。
- 新题允许一个不可读选项；仅在当前图标与可读答案精确、唯一且高可信对应时显示答案。
- 18 个额外补充图标同时使用原图和去装饰边框版本检索、匹配，保留资源校验及独立更新机制。
- 新增“吃茶去了”实际题面裁剪回归，保留旧题失效和图文歧义保护。
- 已知问题：Windows 10 1909 的 windows_capture.pyd 崩溃仍在排查，本版尚未修复。

## [0.4.4] - 2026-09-09

- 教师节全局题面搜索改为缩小图候选搜索与原始分辨率局部校验，缓存缩放模板，减少无题时的 CPU 开销。
- 修复仅凭相似标题就进入“疑似教师节”状态的问题；需要第二个锚点支持，避免普通画面反复搜索并阻止科举分流。
- 保留已定位题框的局部追踪、移动后重新定位和关闭清除，不降低预览帧率与新题扫描频率。
- 补充小题框、2K/4K、新题出现、题框移动/关闭及标题干扰回归；增加与 v0.4.3 对比的本地基准工具。

## [0.4.3] - 2026-09-08

- 修复中文路径下科举定位图无法读取，校准工具同步支持中文路径；便携包自检增加科举布局资源检查。
- 窗口关闭时限制后台请求等待时间，先启动服务退出再通知原生预览，避免退出顺序阻塞。
- OCR 和预览默认显卡优先：OCR 按专用显存顺序尝试 DirectML 显卡，预览使用显示器对应显卡；失败回退 CPU，保留手动 CPU 设置。
- 修复后台显卡自检覆盖真实运行后端状态，以及 provider 检查异常未触发回退的问题。
- 加入 18 条独立教师节补充记录，总计 354 条、353 个技能名；官方题库更新不会覆盖补充库，支持重复记录去重和旧安装播种。
- 增加教师节与科举对比度实验工具、结果和验收文档；科举样本出现退化，未将固定对比度增强启用为默认。

## [0.4.1-beta.1] - 2026-09-02

- 修复教师节任一干扰选项未被题库收录就整题拒绝的问题；完整读取四个选项后，可用高可信且能排除其他图库候选的图像证据定位已知答案。
- 图标证据强且唯一时，按文字相似度给出低可信候选；支持一处错字、漏字、多字或相邻字颠倒，候选并列、图像歧义或已知技能冲突时不框选。
- 选项分数和综合评分计入模糊文字相似度；新增四题真实裁剪回归，包括“鹰击”被 OCR 读成“鷹击”，验证弱框、换位和缺项。
- 发布构建锁定支持许可证元数据的 setuptools，并自动发现 Visual Studio C++ 工具集与 Windows SDK，支持本机和 GitHub Windows 构建环境。

## [0.4.0] - 2026-09-01

- 新增教师节“看图说话”，自动与科举识别切换；内置官方 336 条技能图标记录，支持离线使用。
- 使用标题、退出按钮和题面提示定位答题框，再将图标颜色、内部结构、边缘特征与四个选项联合匹配。
- 同图换选项重新映射；切题、关闭题面和题库更新使旧识别结果失效，未完成或有冲突的题面等待重试。
- “更新题库”分别报告科举和教师节结果；教师节按完整资源快照原子更新，可从上一有效版本或内置资源恢复。
- 旧配置和自定义数据目录自动补齐缺失的教师节资源；两种活动共用一份 OCR 实例。
- 侧栏新增活动、图标分数和两套题库状态；诊断包新增技能图标、候选评分、题库版本和匹配器版本。
- 扩展便携包自检和构建清单；优先覆盖 1024×768 及以上画面，实际验证边界见 `docs/v0.4-validation.md`。

## [Unreleased]

- 使用 pywebview 6.x 和系统 WebView2 Runtime 承载默认 `1440×900`、可缩放的普通桌面窗口，Uvicorn 在后台线程继续提供原有 HTTP/WebSocket 数据流。
- 桌面模式改用随机回环端口；第二次启动会还原并聚焦现有窗口，激活协议不会返回本地 URL 或一次性令牌。
- WebView2 缺失、渲染后端错误或初始页面加载失败时，复用同一个本机服务自动转到系统浏览器；也可用 `--external-browser` 主动进入浏览器模式。
- PyInstaller 继续生成便携式 `onedir`，并显式排除 Qt、CEF、GTK、MSHTML 等非目标渲染后端。
- 采集与预览保持 30 FPS，识别拆为独立节拍：答题框存在时最高 15 FPS，无题界面完成状态清理后以 5 Hz 扫描布局。
- OpenCV 限制为最多 2 个工作线程，RapidOCR 限制 ONNX Runtime 线程数；同一低置信题的重复识别采用最高 2 秒的退避，题面变化时立即恢复快速识别。
- 前端避免每帧重建 Canvas、重复绘制未变化的框选和重复写入未变化的侧栏文本。
- 新增独立的 OCR 与预览后端设置：支持保存到下次启动或确认后立即重启应用；DirectML 设备必须完成真实模型推理自检后才可选择，运行失败只回退 OCR。
- CPU 预览由逐帧 JPEG 改为 1024 像素宽的 I420 平面帧，通过本机 WebSocket 交给 WebView2 WebCodecs `VideoFrame` 直接绘制；保留最新帧优先，不阻塞识别。
- Windows 原生预览辅助进程、命名管道、共享识别帧和运行时首帧超时回退已接入；未完成采集与预览联合门禁的设备不会被自动选择。

## [0.2.0] - 2026-07-15

- 布局分析加入分辨率自适应缩放，同时保留原始分辨率预览与 OCR。
- 题目 OCR 加入精确、扩展和面板题目带多级容错，并将题目识别与选项定位分离。
- 加入同题时间连续性、短暂布局缺失宽限和低置信候选定时重试。
- 候选框改为按综合评分从绿色渐变到红色；低评分虚线半透明，高评分实线加粗。
- 题库按唯一题面分组，同题多答案可结合当前选项消歧，并修复含英文逗号的完整答案匹配。
- 新增独立的本地补题与答案修正存储，支持原子写入、冲突隔离和损坏文件安全降级。
- 复用已检测布局并缩小高分辨率布局分析画面，降低重复计算开销。
- 发布审计明确排除 `user-data`、诊断输出和本地补题 JSON。

验证范围：科举主流程已有实际活动验证；乡试和殿试仍只有兼容性实现与有限样本验证，不承诺覆盖全部现场界面。

## [0.1.0] - 2026-07-14

首个公开候选版本：

- Windows 10/11 x64 便携式 `onedir` 应用和本地浏览器界面。
- Windows Graphics Capture 游戏窗口预览。
- 离线科举题库、题库原子更新和高置信度答案框。
- 普通题、图片题、3～4 选项、多分辨率和可变题面高度识别。
- 会试、殿试布局适配；乡试兼容实现待活动现场验证。
- 单实例激活、一次性浏览器令牌、会话恢复和端口冲突提示。
- 识别诊断、无画面环境诊断、一键自检和构建清单校验。
- GitHub CI 与标签发布流程。
