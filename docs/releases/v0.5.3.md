# XYQQuiz 0.5.3 本地修复版

- 修复首次资源初始化时目录切换遇到 Windows 短暂访问拒绝就直接退出的问题；对访问/共享冲突最多重试六次，总等待约 3.1 秒。
- 在单实例保护内初始化配置和题库，避免正常启动的两个进程同时初始化。
- 发布初始化资源使用不覆盖已有目标的 Windows rename，保留其他初始化进程已经发布的资源；教师节补充资源沿用同一处理。
- 初始化失败显示明确资源错误，不再在日志建立前直接抛出 traceback。
- 保留 0.5.2 性能记录模式。Windows 10 1909 原生捕获崩溃仍未修复。
