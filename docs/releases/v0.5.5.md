# XYQQuiz 0.5.5

- 修正教师节“佛法无边”图标：新增 180° 旋转版本，并从识别索引中排除已确认错误的官方原图。
- 有效官方图标 335 条、独立补充记录 32 条，合计仍为 367 条。补充库按增量合并，保留本地额外记录和旧 generation；官方更新不会覆盖补充记录。
- 保留 0.5.4 的性能记录、标准模式答题期间最高 15 FPS OCR 取帧、首次资源初始化修复，以及低配模式和统一框选样式。
- README、包内说明、开发交接和应用/PE 版本同步为 0.5.5。

请完整解压到新目录运行，不要覆盖旧版 EXE 或 `_internal`。可按需迁移配置与用户数据；首次启动会合并缺失的补充记录。性能记录需在退出前导出，操作见[性能记录说明](../performance-recording.md)。

本次图标修正有对应的题库和真实裁剪回归。自动测试与便携包自检不能替代实际活动连续答题验收。Windows 10 1909 的 `windows_capture.pyd / 0xC0000409` 原生捕获崩溃仍未修复。
