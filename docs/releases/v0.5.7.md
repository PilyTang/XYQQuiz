# XYQQuiz 0.5.7

- 修复教师节一个干扰选项 OCR 置信度较低时，连带关闭其他选项近似匹配、导致漏报的问题。
- 四个已有文字的选项统一比较相似度与分差；例如图标为“煞气诀”、选项为“煞气决”时，只要明显领先其他选项，即可显示候选虚线框。
- 保留图标可信门槛、答案选项自身的文字可信要求，以及并列、空白和歧义时不画框的保护。
- 加入真实诊断图标回归，覆盖低可信干扰项、选项换序、低可信答案、文字并列与空白选项。

这是包含识别修复的正式 0.5.7，替代之前仅用于更新提示验证的同名测试草稿。请完整解压到新目录运行；CNB 与 GitHub 发布相同的安装包和 SHA-256 校验文件。

保留 0.5.6 的 CNB 更新检查与启动新版提示。Windows 10 1909 原生捕获崩溃仍未修复。
