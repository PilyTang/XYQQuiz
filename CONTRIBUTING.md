# 贡献指南

感谢参与 XYQQuiz。提交代码前请遵守以下边界：

- 不加入自动点击、游戏输入、进程内存读取或注入功能。
- 不提交真实游戏全屏截图、角色名、服务器信息、聊天内容或个人绝对路径。
- 新识别逻辑必须在信息不足时安全降级为无答案框。
- 按行为改动的风险和现有覆盖情况补充或更新有实际价值的测试；文案、文档及低影响的可逆修改不强制新增测试。

开发环境与测试命令见 [README.md](README.md)。日常修改优先运行与改动直接相关的检查，不强制每次修改或提交都运行全量测试和公开树检查。涉及公开文件边界时运行公开树检查；跨模块改动、发布验证或现有失败需要时运行全量测试。可按需使用以下命令：

```powershell
.venv\Scripts\python.exe .\scripts\check_public_tree.py
.venv\Scripts\python.exe -m pytest -q
```

真实截图回归只在本地私有 fixture 目录运行。Issue 或 Pull Request 中请使用脱敏截图、合成样本或最小文本复现。

## 正式发布规则

只有用户明确要求正式发布更新时，才执行 [正式发布流程](docs/release-process.md)：同步项目说明和版本、验证、更新 GitHub 代码、发布 GitHub Release、发布 CNB、最后更新 `latest.json` 并核对两端文件一致。普通修复、诊断、测试或本地打包不自动触发发布，过去的发布授权也不延续到下一次修改。
